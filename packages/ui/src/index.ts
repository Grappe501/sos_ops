/**
 * SOS OPS — UI Package
 *
 * Shared, framework-agnostic UI primitives.
 *
 * This package intentionally exports nothing yet.
 * Components and hooks will be added in later modules.
 *
 * Constraints:
 * - No side effects at import time
 * - No framework coupling beyond React
 */

// Placeholder export to keep the package valid.
export {};
